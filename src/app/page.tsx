import Link from "next/link";
import React from "react";

function page() {
    return (
        <header>
            <Link href={"/hook-form"}>Hook Form</Link>
        </header>
    );
}

export default page;
